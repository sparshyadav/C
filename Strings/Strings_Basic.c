#include <stdio.h>

int main()
{

    // char arr[] = {'H', 'e', 'l', 'l', 'o'};
    // int i = 0;
    // while (arr[i] != '\0')
    // {
    //     printf("%c ", arr[i]);
    //     i++;
    // }
    char arr[] = {"Hello World!"};
    int i = 0;
    while (arr[i] != '\0')
    {
        printf("%c", arr[i]);
        i++;
    }

    return 0;
}