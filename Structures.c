#include<stdio.h>
#include<string.h>

struct point{
    int x;
    int y;
};

struct rectangle{
    struct point ll;
    struct point lr;
    struct point ul;
    struct point ur;
};

struct circle{
    struct point center;
    struct point radius;
};

struct rectarea(struct rectange )
    int length=lr
int main(){



    return 0;
}